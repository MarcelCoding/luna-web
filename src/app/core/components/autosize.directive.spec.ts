import { AutosizeDirective } from './autosize.directive';

xdescribe('AutosizeDirective', () => {
  it('should create an instance', () => {
    // const directive = new AutosizeDirective();
    // expect(directive).toBeTruthy();
  });
});
