import { ContentEditableDirective } from './content-editable.directive';

xdescribe('ContentEditableDirective', () => {
  it('should create an instance', () => {
    // const directive = new ContentEditableDirective();
    // expect(directive).toBeTruthy();
  });
});
